import React from 'react';
import ReactDOM from 'react-dom/client';
import BuildingDashboard from './dashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BuildingDashboard />
  </React.StrictMode>
); 